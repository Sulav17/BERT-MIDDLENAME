# Copy this file to .env and adjust environment variables as needed

# API server port
PORT=8000
# Path to the fine-tuned model directory (optional)
# Defaults to ./models/bert-middle-name
MODEL_PATH=./models/bert-middle-name